export const GRAPH = {
    X_START: 0,
    X_END: 100,
    Y_START: 0,
    Y_END: 100
};

export enum AXIS_TYPE {
    DATE = 'date',
    NUMBER = 'number',
    STRING = 'string'
}

export enum AXIS {
    X = 'x',
    Y = 'y'
}